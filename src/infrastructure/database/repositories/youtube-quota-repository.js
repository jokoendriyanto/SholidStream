'use strict';
class YoutubeQuotaRepository{constructor(pool){this.pool=pool;}async consume({workspaceId,connectionId=null,operation,units,correlationId=null,dailyLimit}){const client=await this.pool.connect();try{await client.query('BEGIN');await client.query(`INSERT INTO youtube_quota_daily_totals(workspace_id,quota_day,used_units) VALUES($1,CURRENT_DATE,0) ON CONFLICT(workspace_id,quota_day) DO NOTHING`,[workspaceId]);const total=await client.query(`SELECT used_units FROM youtube_quota_daily_totals WHERE workspace_id=$1 AND quota_day=CURRENT_DATE FOR UPDATE`,[workspaceId]);const used=Number(total.rows[0]?.used_units||0);if(Number.isFinite(dailyLimit)&&used+units>dailyLimit){const error=new Error('YouTube quota budget exceeded');error.code='YOUTUBE_QUOTA_BUDGET_EXCEEDED';throw error;}await client.query(`UPDATE youtube_quota_daily_totals SET used_units=used_units+$2,updated_at=NOW() WHERE workspace_id=$1 AND quota_day=CURRENT_DATE`,[workspaceId,units]);await client.query(`INSERT INTO youtube_quota_usage(workspace_id,connection_id,operation,units,correlation_id) VALUES($1,$2,$3,$4,$5)`,[workspaceId,connectionId,operation,units,correlationId]);await client.query('COMMIT');return{used:used+units,remaining:Number.isFinite(dailyLimit)?Math.max(0,dailyLimit-used-units):null};}catch(e){await client.query('ROLLBACK').catch(()=>{});throw e;}finally{client.release();}}}
module.exports={YoutubeQuotaRepository};
