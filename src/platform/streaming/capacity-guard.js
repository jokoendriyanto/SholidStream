'use strict'; class CapacityGuard{constructor({pool,entitlementService,enforced=true}={}){this.pool=pool;this.entitlementService=entitlementService;this.enforced=enforced;}async assertCanStart(workspaceId){if(!this.enforced)return{enforced:false};const r=await this.pool.query(`SELECT COUNT(*)::int AS count FROM stream_sessions WHERE workspace_id=$1 AND state IN ('QUEUED','ALLOCATING','PREPARING','STARTING','CONNECTING','LIVE','DEGRADED','RECOVERING','STOPPING')`,[workspaceId]);const current=r.rows[0]?.count||0;await this.entitlementService.assertLimit(workspaceId,'streams.concurrent',current,1);return{enforced:true,current};}}
module.exports={CapacityGuard};
