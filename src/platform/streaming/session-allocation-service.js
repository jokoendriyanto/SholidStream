'use strict';
const crypto=require('crypto'); const {STREAM_STATE}=require('../../domain/streaming/stream-state'); const {createStartSessionCommand}=require('./worker-command'); const {workerQueueName}=require('./worker-routing');
class SessionAllocationService{
  constructor({sessionRepository,eventRepository,allocator,queueRegistry,planBuilder}={}){if(!sessionRepository||!eventRepository||!allocator||!queueRegistry||typeof planBuilder!=='function')throw new TypeError('SessionAllocationService dependencies are required');Object.assign(this,{sessionRepository,eventRepository,allocator,queueRegistry,planBuilder});}
  async allocate({workspaceId,sessionId,preferredRegion=null}){let session=await this.sessionRepository.findById(workspaceId,sessionId);if(!session)throw Object.assign(new Error('Stream session not found'),{code:'STREAM_SESSION_NOT_FOUND'});if(session.state===STREAM_STATE.QUEUED||session.state===STREAM_STATE.RECOVERING){const from=session.state;session=await this.sessionRepository.transition({workspaceId,sessionId,expectedState:from,expectedVersion:session.version,toState:STREAM_STATE.ALLOCATING});await this.eventRepository.append({workspaceId,sessionId,eventType:'CONTROL_PLANE_ALLOCATING',fromState:from,toState:STREAM_STATE.ALLOCATING,generation:session.generation,metadata:{preferredRegion}});}if(session.state!==STREAM_STATE.ALLOCATING)throw Object.assign(new Error(`Session cannot allocate from ${session.state}`),{code:'STREAM_SESSION_NOT_ALLOCATABLE'});
    const {worker,reservation}=await this.allocator.allocate({sessionId,generation:session.generation,preferredRegion});session=await this.sessionRepository.assignWorker({workspaceId,sessionId,expectedVersion:session.version,workerId:worker.id,reservationId:reservation.id});const leaseToken=crypto.randomBytes(32).toString('hex');const plan=await this.planBuilder({workspaceId,session});const command=createStartSessionCommand({sessionId,workspaceId,definitionId:session.stream_definition_id,generation:session.generation,leaseToken,sources:plan.sources,outputs:plan.outputs,encodingProfile:plan.encodingProfile||{},runtimePolicy:plan.runtimePolicy||{}});await this.queueRegistry.get(workerQueueName(worker.id)).add('START_SESSION',{command},{jobId:`start-${sessionId}-g${session.generation}`});await this.eventRepository.append({workspaceId,sessionId,eventType:'WORKER_ASSIGNED',fromState:session.state,toState:session.state,generation:session.generation,metadata:{workerId:worker.id,reservationId:reservation.id}});return{session,worker,reservation,command};}
}
module.exports={SessionAllocationService};
