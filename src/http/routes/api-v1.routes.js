'use strict'; const express=require('express'); const {requireScope}=require('../middleware/api-key-auth');
function createApiV1Router({pool,apiKeyAuth,adminService,entitlementService}={}){if(!pool||!apiKeyAuth||!adminService)throw new TypeError('API v1 dependencies are required');const router=express.Router();router.use(apiKeyAuth);router.get('/me',(req,res)=>res.json({apiKey:{id:req.apiKey.id,name:req.apiKey.name,workspaceId:req.apiKey.workspace_id,scopes:req.apiKey.scopes,platformAdmin:req.apiKey.platform_admin}}));router.get('/streams',async(req,res)=>{if(!req.workspaceId)return res.status(400).json({error:'workspace_required'});const result=await pool.query(`SELECT id,stream_definition_id,state,generation,assigned_worker_id,started_at,stopped_at,created_at FROM stream_sessions WHERE workspace_id=$1 ORDER BY created_at DESC LIMIT 100`,[req.workspaceId]);res.json({items:result.rows});});router.get('/workers',requireScope('workers:read'),async(req,res)=>{const result=await pool.query(`SELECT id,worker_key,region,status,max_streams,active_streams,reserved_streams,cpu_percent,memory_percent,last_heartbeat_at FROM worker_nodes ORDER BY region,worker_key`);res.json({items:result.rows});});router.get('/billing/entitlements',async(req,res)=>{if(!entitlementService||!req.workspaceId)return res.json({items:[]});const map=await entitlementService.snapshot(req.workspaceId);res.json({items:Array.from(map.entries()).map(([featureKey,value])=>({featureKey,...value}))});});router.get('/admin/overview',requireScope('admin:platform'),async(req,res)=>res.json(await adminService.overview()));return router;}
module.exports={createApiV1Router};
