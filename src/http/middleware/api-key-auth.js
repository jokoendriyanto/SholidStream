'use strict'; function createApiKeyAuth({repository}={}){if(!repository)throw new TypeError('API key repository is required');return async function apiKeyAuth(req,res,next){try{const header=req.headers.authorization||'';const token=header.startsWith('Bearer ')?header.slice(7):null;const key=await repository.authenticate(token);if(!key)return res.status(401).json({error:'unauthorized'});req.apiKey=key;req.workspaceId=key.workspace_id;return next();}catch(error){return next(error);}};}function requireScope(scope){return function(req,res,next){if(req.apiKey?.platform_admin||req.apiKey?.scopes?.includes(scope))return next();return res.status(403).json({error:'forbidden',requiredScope:scope});};}module.exports={createApiKeyAuth,requireScope};
