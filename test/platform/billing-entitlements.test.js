'use strict'; const test=require('node:test'); const assert=require('node:assert/strict'); const {canTransitionSubscription,assertSubscriptionTransition}=require('../../src/billing/subscription-state-machine'); const {EntitlementService}=require('../../src/billing/entitlement-service');
test('subscription state machine blocks impossible reactivation after cancellation',()=>{assert.equal(canTransitionSubscription('ACTIVE','PAST_DUE'),true);assert.equal(canTransitionSubscription('CANCELLED','ACTIVE'),false);assert.throws(()=>assertSubscriptionTransition('CANCELLED','ACTIVE'),e=>e.code==='INVALID_SUBSCRIPTION_TRANSITION')});
test('entitlement service enforces feature limits',async()=>{const service=new EntitlementService({query:async()=>({rows:[{feature_key:'streams.concurrent',enabled:true,limit_value:'2'}]})});await service.assertLimit('w1','streams.concurrent',1,1);await assert.rejects(()=>service.assertLimit('w1','streams.concurrent',2,1),e=>e.code==='ENTITLEMENT_LIMIT_EXCEEDED')});
