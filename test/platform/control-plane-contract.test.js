'use strict'; const test=require('node:test'); const assert=require('node:assert/strict'); const fs=require('fs'); const path=require('path');
test('production control-plane bootstrap and compose services exist',()=>{const root=path.join(__dirname,'../..');const cp=fs.readFileSync(path.join(root,'control-plane.js'),'utf8');const compose=fs.readFileSync(path.join(root,'docker-compose.saas.yml'),'utf8');assert.match(cp,/RuntimeWatchdog/);assert.match(cp,/DistributedScheduler/);assert.match(compose,/control-plane:/);assert.match(compose,/stream-worker:/)});
test('legacy encryption no longer contains the default-secret fallback',()=>{const content=fs.readFileSync(path.join(__dirname,'../../utils/encryption.js'),'utf8');assert.doesNotMatch(content,/default-secret-key-change-me/);assert.match(content,/DATA_ENCRYPTION_KEY/)});
